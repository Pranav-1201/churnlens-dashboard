[pytest]
testpaths = tests
# The full train-and-publish curve test trains the whole model zoo (~13 min).
# It is excluded from the default run; CI and pre-release run it explicitly:
#     pytest -m slow
addopts = -m "not slow"
markers =
    slow: trains real models end-to-end; run with `pytest -m slow`
